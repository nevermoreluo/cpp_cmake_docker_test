#include "IdGenerator.h"
/*#include <limits>

template <typename T>
T CIdGenerator<T>::Generate()
{
	if (m_nNextGuid >= (std::numeric_limits<T>::max() - 1))
	{
		// 日志并且结束程序
	}

	return m_nNextGuid++;
}*/
